import styles from "@/styles/layout/Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.container}>
      <p>Thanks for taking a Marvel Approach.</p>
    </footer>
  );
};

export default Footer;
